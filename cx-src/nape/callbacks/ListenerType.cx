package nape.callbacks;

import nape.util.Flags;
import zpp_nape.util.Flags;
import zpp_nape.util.Names;

Flags(ListenerType,
    prop(BODY,0)
    prop(CONSTRAINT,1)
    prop(INTERACTION,2)
	prop(PRE,3)
)

