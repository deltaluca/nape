package nape.shape;
$(import);

!!/**
!! * Enumeration of Nape Shape types.
!! */
Flags(ShapeType,
    !!/**
    !! * Circle shape type
    !! */
    prop(CIRCLE,0)

    !!/**
    !! * Polygon shape type
    !! */
    prop(POLYGON,1)
)

