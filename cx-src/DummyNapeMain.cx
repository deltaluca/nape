package;
$(import);

class DummyNapeMain {
    static function main() {
        var space = new Space(new Vec2(0, 400));
        var c = new PivotJoint(null, null, new Vec2(), new Vec2());
        var d = new Compound();
        c.space = space;
        c.compound = d;
        d.space = space;
    }
}
