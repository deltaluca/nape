package;
$(import);

class DummyNapeMain {
    static function main() {
        new DummyNapeMain();
    }

    function new() {
        var shape = new Polygon(Polygon.rect(0,0,50,50));
        trace(shape.pr(inner).inv(localCOM));
        trace(shape.localCOM);
        trace(shape.pr(inner).inv(localCOM));
    }
}
