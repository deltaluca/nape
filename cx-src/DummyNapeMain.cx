package;
$(import);
import flash.Lib;
class DummyNapeMain
{
    static function main()
    {
        var pt = Lib.getTimer();
        var sum = 0;
        for (i in 0...1000000)
        {
            sum += (function (j) return (j%2)==0 ? 1 : 0)(i);
//            sum += (i%2)==0 ? 1 : 0;
        }
        trace(Lib.getTimer()-pt);
    }
}
