
package;
$(import);


class DummyNapeMain {
    static function main() {
        var p = new GeomPoly(Polygon.box(20, 20));
        trace(0/0);
        trace(p);
        trace(p.inflate(5));
        trace(p.inflate(-5));
    }
}
