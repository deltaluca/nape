package;
$(import);


class DummyNapeMain {
    static function main() {
        function iso(x:Float, y:Float) { return y - 50; }

        var polys = MarchingSquares.run(iso, new AABB(0, 0, 100, 100), new Vec2(10, 10), 2);
        trace("--");
        var polys = MarchingSquares.run(iso, new AABB(0, 0, 100, 100), new Vec2(10, 10), 2);
        trace("--");
        var polys = MarchingSquares.run(iso, new AABB(0, 0, 100, 100), new Vec2(10, 10), 2);
    }
}
