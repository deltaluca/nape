package;
$(import);

class DummyNapeMain {
    static function main() {
        var gp = new GeomPoly([new Vec2(0, 0), new Vec2(10, 0), new Vec2(5, 5), new Vec2(10, 20), new Vec2(0, 20), new Vec2(5, 5)]);
        trace(gp);
        trace(gp.simpleDecomposition());
        trace(gp.triangularDecomposition());
    }
}
