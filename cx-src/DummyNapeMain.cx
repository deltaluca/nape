package;
$(import);
import flash.Lib;
class DummyNapeMain
{
    static function main()
    {
        var p = new Polygon(Polygon.box(10, 20));
        trace(p.localVerts);
        p.localVerts.clear();
        trace(p.localVerts);
        for (x in Polygon.box(10, 20)) p.localVerts.add(x);
        trace(p.localVerts);
        p.localVerts.clear();
        trace(p.localVerts);
    }
}
