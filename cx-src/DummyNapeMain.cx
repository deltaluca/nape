package;
$(import);

class DummyNapeMain {
    static function main() {
        var v = new Vec2();
        v.dispose();
        try {
            v.dispose();
        }
        catch (e:Dynamic) {
            trace(Type.getClassName(e));
        }
    }
}
