package;
$(import);
import flash.Lib;
class DummyNapeMain
{
    static function main()
    {
        var space = new Space(null, Broadphase.SWEEP_AND_PRUNE);
        space.bodiesUnderPoint(new Vec2(10, 20));
    }
}
