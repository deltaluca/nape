package;
$(import);


class DummyNapeMain {
    static function main() {
        var b1 = new Body();
        var b2 = new Body();
        b1.shapes.add(new Circle(10));
        b2.shapes.add(new Circle(10));
        var c = new PivotJoint(b1, b2, new Vec2(), new Vec2());
        var s = new Space();
        c.space = b1.space = b2.space = s;
        c.maxForce = 0.00001;

        s.step(1/100, 10, 10);
        s.step(1/100, 10, 10);
        s.step(1/100, 10, 10);
    }
}
