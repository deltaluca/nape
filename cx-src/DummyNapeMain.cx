package;
$(import);

class DummyNapeMain {
    static function main() {
        var b = new Material();
        trace(b.userData);
        b.userData.vec = new Vec2(1,0);
        trace(b.userData);
        trace(b.userData.vec);
    }
}
