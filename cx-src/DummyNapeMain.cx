package;
$(import);

class DummyNapeMain {
    static function main() {
        new DummyNapeMain();
    }

    function new() {
        var shape = new Polygon(Polygon.rect(0,0,50,50));
        var body = new Body();
        body.shapes.add(shape);
        trace(body.localCOM);
    }
}
