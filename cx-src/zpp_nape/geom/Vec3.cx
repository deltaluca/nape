package zpp_nape.geom;
$(import);

class PR(Vec3) {
	public var outer:Vec3;

	vec3_new(public,$(->));

	public var immutable:Bool;
	public var validate:Void->Void;

	public function new() {
		immutable = false;
		validate = null;
	}
}
