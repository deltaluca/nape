package zpp_nape;
$(import);

class PR(Const) {
	#if flash
		//cannot use POSITIVE_INFINITY due to haxe bug
		//cannot use untyped __global__["Number"].POSITIVE_INFINITY due to some strange memory consumption.
		public static var POSINF =  1.79e+308;
		public static var NEGINF = -1.79e+308;
	#else
		public static var POSINF = Math.POSITIVE_INFINITY;
		public static var NEGINF = Math.NEGATIVE_INFINITY;
	#end
	public static var EPSILON = 1e-8;
	public static var FMAX = 1e100;
}
