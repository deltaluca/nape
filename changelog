Fix for adding a static object to a space, overlapping sleeping objects.
    Previously sleeping objects would not be woken by addition of static object.
    This effected only DYN_AABB broadphase (default), and should now be fine.

Added nape.Config to permit tuning of ALL physics constants.
